<%- include('partials/head.ejs') %>
  <body>
    <%- include('partials/homenavbar.ejs') %>

    <div class="container">
      <div class="bg-warning mt-3 mb-3  p-3">
        <h1 class="display-4">Bem vindo</h1>
        <p class="lead">
          Este é um simples blog, desenvolvido como meio de estudo para 
          desenvolvimento web.
        </p>
        <hr class="my-4">
        <p>Nele usamos as tecnologias: Node, EJS, MYSQL, Sequelize, Bootstrap e Tinymce</p>
      </div>

      <% articles.forEach(function(article) { %>
        <div class="card mb-3">
          <div class="card-header">
            <h1><%= article.title %></h1>
          </div>
          <div class="card-body">
            <a class="btn btn-success" href="/<%= article.slug %>">Ler</a>
          </div>
        </div>
      <% }) %>
    </div>

    <%- include('partials/scripts.ejs') %>
  </body>
</html>